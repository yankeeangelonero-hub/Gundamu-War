SUCCESS: The process with PID 635332 (child process of PID 462264) has been terminated.
**P1 Findings**

1. **CG-SUSPENSE is not implementation-ready as a hard invariant.** In `Two universal laws` and `Testing`, the rule says staged motion must not reveal the winner before `climax_window`, enforced by an “independent naive predictor” over the motion trace ([design](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:45>), [test](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:186>)). That predictor is not specified: no frozen feature set, model class, training corpus, calibration, version, evaluation window, tie/draw handling, or per-fight vs corpus-level pass rule. This is gameable by choosing a weak predictor and circular if tuned against the choreographer’s own generated distribution.

2. **CG-SUSPENSE is unachievable for some valid truths unless the rule is made vacuous.** The spec still allows fast kills and stomps ([design](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:56>)), but also says every fight should not be knowable “three seconds in” ([goal](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:26>)). A valid early lethal, repeated early heavy hits, or immediate destruction cannot be hidden from motion without either faking landed outcomes or producing incoherent sells. Setting `climax_window` at/near the first lethal would pass the letter while breaking the owner intent.

3. **The new presentation namespace is not consistent with the frozen event-log contract as written.** Rev 2 says “The loader/projection contract is updated to skip the presentation namespace” ([seam](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:126>)), but the contract under context is frozen. Its root shape only defines `events`, and every event has `{tick, seq, actor, kind, payload}` ([contract](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-17-fight-event-log-contract-design.md:95>), [event shape](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-17-fight-event-log-contract-design.md:110>)). If hooks are root-level metadata, the schema must say so; if they are events, “carry no seq” conflicts with the event shape.

**P2 Findings**

4. **The scheduler is deterministic in intent, not in spec.** “Coalescing near-simultaneous shots of the same exchange,” lethal/heavy ranking, background demotion, and “one committed primitive per actor” are all underspecified ([scheduler](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:101>)). Missing: coalesce window, “same exchange” identity, heavy threshold, tie-break by `(tick, seq)`, actor conflicts, simultaneous lethal cases, and how background fire preserves hit/miss readability.

5. **Mode-weight composition is still ambiguous.** Layer 3 says exchange mode is selected from weapon mode weights × `FeelProfile.mode_mix` ([architecture](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:84>)), while extensibility says weapon weights are over grammar modes and FeelProfile modes map through a data table ([extensibility](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:147>)). It never defines the actual operation: multiply then normalize, blend, actor attacker vs defender profile, zero-weight behavior, or many-to-one/new feel-mode rows.

6. **CG-CONTRAST is improved but still can false-fail legitimate beam exchanges.** The global bimodal spine is gone, but beam-trade still hard-gates on a bimodal speed histogram plus pauses for any active beam-trade ([contrast](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:38>), [testing](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:183>)). A frantic close-range or high-tempo beam duel may be readable through aim alternation, recoil, and impact sells rather than pause clusters. Also, melee has no named contrast metric yet, and swarm/pursuit are collapsed into one bearing-oscillation metric.

7. **The seam is less leaky, but per-fight hooks still need identity rules.** Per-beat `truth_ref` is a real fix ([seam](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:116>)). But `phrase_bounds[]`, `apparent_initiative[]`, and `climax_window` are ranges/timelines, not single truth beats; the blanket statement that each hook carries `truth_ref:{tick,seq}` is not actually defined for them ([seam](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:122>)).

**P3 Findings**

8. **The lean file set is smaller but path/data-resource details are not build-ready.** The spec lists `scripts/sim/...` paths ([scope](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:165>)), while shipped specs and current files use `godot_director_spike/scripts/...`. It also names a `grammar_presets` data resource but does not specify its path, schema, or loader ownership.

**Resolution Check**

- Dramaturgy demanding truth changes: **resolved for the original issue**, but CG-SUSPENSE reintroduces an impossible hard gate for some valid truths.
- Hooks lacking deterministic identity: **partially resolved**; per-beat fixed, per-fight hooks still ambiguous.
- FeelProfile determinism: **mostly resolved** by required profiles and version pinning ([architecture](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:69>)); PvP presentation replay remains version-local and underspecified.
- Spine over-promoted: **partially resolved**; no global bimodality, but beam-trade still over-gated.
- “Every shot has CRA”: **resolved in intent** by background-fire demotion, but scheduler rules are not precise enough.
- Feel-mode/grammar-mode mapping conflict: **partially resolved**; table exists, composition semantics do not.
- Leaky boundary: **mostly resolved** for dwell/render flags; namespace/schema conflict remains.
- Proxy metrics: **not resolved for CG-SUSPENSE**; partially resolved for contrast/range.
- Over-decomposition: **resolved structurally**, with minor buildability/path gaps.

**Verdict**

Still needs another pass before implementation. The blocking issue is CG-SUSPENSE: as written it is neither a well-defined test nor guaranteed achievable for valid deterministic fight logs. The contract namespace issue also must be resolved against the frozen event-log shape before hooks can be added safely.

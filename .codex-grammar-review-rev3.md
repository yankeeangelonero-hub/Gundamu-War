SUCCESS: The process with PID 409360 (child process of PID 575976) has been terminated.
**P1**
1. CG-NO-PRESPOIL is still not an implementation-ready hard gate. In [rev3 Testing](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:199>), `truth_dom`, `staged_dom`, and `reveal()` are named, but not fully specified. Missing: normalization denominator, handling of `melee`, `post_decision`, asymmetric spawn HP, cap/stalemate winners, draw/zero-damage `reveal = never`, sign epsilon, and same-tick `(tick,seq)` sampling. Worse: `staged_dom` depends on choreographer-authored `apparent_initiative`, so a broken choreographer can emit neutral initiative while its actual movement telegraphs the winner. The validator must compute staged dominance from raw staged trace, or independently validate `apparent_initiative` against movement.

2. The beat scheduler is still not total. [rev3 Beat Scheduler](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:102>) defines coalescing, rank, commitment, and simultaneous lethal, but `cue_tick` is not defined, coalescing depends on “selected grammar-mode” before the beat exists, and overlap resolution does not say whether a later lethal/heavy beat can preempt an earlier normal committed span. As written, a normal commitment can demote a later lethal shot to background.

**P2**
1. Root `presentation` outside `events` resolves the frozen event shape, but not the contract by itself. The current contract root is fixed to `{schema,tick_seconds,seed,builds,result,events}` in [contract Root](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-17-fight-event-log-contract-design.md:95>), and INV-VERIFY excludes presentation events/fields, not a root presentation object in [contract INV-VERIFY](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-17-fight-event-log-contract-design.md:72>). The amendment must explicitly update root schema, loader unknown-key policy, outcome projection, and canonical serialization/hash behavior. Also require sorted unique `apparent_initiative` ticks and finite/quantized `lead`.

2. CG-EXCITING-SHAPE is still mostly a promise. [rev3 lines 48-50](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:48>) name `hero-stomp`, but there is no shape-classifier algorithm and no template registry for close, reversal, fast-kill, or stalemate. The fixture suite in [rev3 lines 210-217](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:210>) says “correct template” but does not define expected template IDs or `decided_tick`.

3. Mode composition is close but not total. [rev3 Exchange-Mode Composition](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:117>) needs validation for finite nonnegative weights, missing mode keys, NaN, sum drift, and `MODE_MAP` completeness for every open FeelProfile key. First pass also says only beam-trade is implemented in [rev3 Scope](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:170>), but the selector can choose swarm/dodge-pursuit/melee unless disabled or mapped.

**P3**
1. Paths are corrected as proposed implementation targets, but not yet present: `grammar_params.gd`, `movement_trace.gd`, and `data/grammar_presets.json` do not exist in the current spike. The `grammar_presets` schema is stated in [rev3 lines 174-178](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:174>), but `MODE_MAP` still lacks a file path/schema.

**Resolution Check**
- CG-SUSPENSE definability + achievability: PARTIAL. Availability is correctly scoped to sim/balance; the hard gate is not yet test-independent or total.
- Presentation namespace vs frozen contract: PARTIAL. Event shape preserved; root contract amendment is underspecified.
- Scheduler precision: PARTIAL. Deterministic intent exists; overlap/preemption/cue derivation remain open.
- Mode-weight composition: PARTIAL. Core formula exists; total input validation and first-pass unimplemented modes remain open.
- Beam-trade false-fail: RESOLVED. Bimodality OR aim/recoil alternation covers the point-blank false-fail.
- Per-fight hook identity: PARTIAL. Spans/lists replace fake `{tick,seq}` identity; ordering/cardinality of series still needs pinning.
- Paths/resource: PARTIAL. Root path corrected; resource/schema incomplete in repo terms.

**Simplicity**
Rev 3 did not overshoot mainly by adding precision; it underspecified the hardest new machinery. The edge suite and no-prespoil metric are appropriate for a first implementation only after the metric is independent, `decided_tick` is exact, and the scheduler has a total conflict rule.

Verdict: NEEDS-ANOTHER-PASS — single biggest blocker: CG-NO-PRESPOIL can still pass because the choreographer authors the dominance signal being tested.

SUCCESS: The process with PID 361992 (child process of PID 281512) has been terminated.
**Resolution Check**

- Self-authorized reveal: **RESOLVED**. Hard gate uses validator-derived `late_gate`, and `climax_window` is advisory only ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:276>)).
- `staged_dom` channel coverage: **PARTIAL**. Sells were added, and director-owned grade/framing/hero-kill are correctly scoped out, but choreographer-owned beat prominence still escapes.
- Shape stalemate bug: **RESOLVED**. `stalemate` now precedes `photofinish`, and `photofinish` requires `has_kill` ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:160>)).
- Scheduler cue/commit + victim reaction: **PARTIAL**. Cue no longer forward-references commit, and target reaction spans are claimed, but partial reaction demotion has no serializable state.
- EMA/hysteresis determinism: **PARTIAL**. Hysteresis is explicit; `closing_rate_X` and `sells_X` are still not exact enough for independent validators ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:262>)).
- Contract loader/hash semantics: **RESOLVED**. Truth-set root keys and hash exclusion semantics are exact ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:201>)).

**P1 Findings**

`CG-NO-PRESPOIL` is still bypassable through a choreographer-owned dominance channel: foreground/background/full-CRA beat emphasis. The scheduler says background beats get no CRA and that full beats claim cue/action/reaction spans ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:124>)), and the hook exposes `is_background` ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:183>)). But `staged_dom` only reads closing pressure plus sells ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:262>)). A choreographer can foreground the eventual winner’s attacks and suppress the loser’s attacks before truth reveal while keeping pressure and sells neutral. That is not director-owned grade/framing; it is the choreographer’s scheduler output.

**P2 Findings**

The no-pre-spoil math is not fully deterministic. “X’s per-tick reduction of `dist_to_enemy` by its own motion” does not define the counterfactual when both actors move in the same tick. `sells_X` also lacks a concrete detector: raw trace positions do not by themselves distinguish a staged sell from ordinary retreat/evasion unless the formula names the impact window, direction test, threshold, and EMA seed/window.

The scheduler has a representation hole for target reaction conflicts. Step 3 says if the target is already committed to its own beat, “only its reaction is demoted” ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:127>)). The public beat shape has only `is_background: bool`, so it cannot encode “foreground shooter span, background target reaction.” Equal-priority ordering itself is deterministic; this state model is the problem.

Mutual same-tick lethal is only safe if the later lethal is forced to `post_decision` with no damage. Rev 5 says later same-tick shots are “background (or `post_decision` per the contract)” ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:134>)); background is presentation-only and would not stop `truth_dom` from counting damage.

**P3 Findings**

The classifier still references `has_kill` and “stable sign-changes” without local definitions ([spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:156>)). The zero-damage-draw bug is closed, but fixture authors can still diverge on reversal/grind boundaries.

`staged_dom` is **not** faithful enough yet. For beam-trade specifically, cue prominence, foregrounded shots, and background suppression are core readable dominance channels, and they are choreographer-controlled.

Not implementation-ready for a first beam-trade plan.

NEEDS-ANOTHER-PASS + biggest blocker: `CG-NO-PRESPOIL` still ignores choreographer-owned foreground/background/CRA dominance.

SUCCESS: The process with PID 384288 (child process of PID 648672) has been terminated.
**Resolution Check**
| Rev-5 finding | Verdict | Reason |
|---|---:|---|
| Beat-prominence channel | PARTIAL | CG-BLIND names full-CRA/background prominence, but still allows structural staging to read `damage`, `lethal`, and `outcome` [spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:93>). Those are post-impact truth facts; the visible CRA cue can begin before impact. |
| `closing_rate` / sells determinism | PARTIAL | `closing_rate_X` is now separable and explicit [spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:295>). Sells is still under-specified on `shooter_pos` timing and zero-length `unit(...)`. |
| Reaction representation | RESOLVED | `is_background` and `reaction_background` are split in scheduler and schema [spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:151>). |
| Mutual same-tick lethal | RESOLVED | Damage/post-decision ownership is explicitly SIM/INV-ORDER; choreographer stages received truth [spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:160>). |
| Classifier terms | RESOLVED | `has_kill` and `lead_flips` are locally defined [spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:185>). |

**P1**
CG-BLIND does not fully close the beat-prominence pre-spoil channel. The event-log contract says `shot.tick` is impact, with `fire_tick = impact_tick - travel`, and `travel >= 1` [contract](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-17-fight-event-log-contract-design.md:26>). Rev 6 lets full-CRA/prominence depend on `damage/lethal/outcome`, and the scheduler prioritizes lethal/heavy while claiming the shooter span from `cue_tick..impact_tick` [spec](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:148>). That can foreground the eventual kill shot before the hit has landed. It is “attribute-driven,” but the attribute is future resolution truth.

The mirror/byte-identical test is not sufficient. It proves no explicit “winner A/B” branch if the fixture transform is defined correctly; it does not prove temporal prefix-blindness. A pair of mirrored fixtures can both foreground the future lethal cue, compare structurally identical under actor swap, and still pre-announce the kill before `truth_dom` reveals it. It is also not well-defined as written: raw byte identity conflicts with actor swapping unless actor labels are canonicalized, and “swap winner while keeping per-beat attributes identical” needs a concrete fixture transform.

Required design fix: split allowed structural inputs by display time. Pre-impact cue/prominence may use weapon/profile/tier/travel/range/scheduling facts knowable before impact, but not `outcome/damage/lethal/hp_after`. Those may drive impact/reaction/sell and post-reveal hero treatment only at or after the relevant impact/reveal tick. Add a prefix-blindness test, not just a mirrored-winner test.

**P2**
Remaining totality holes are implementation/test-pin level once P1 is fixed: define `unit(zero)` behavior, time-index `shooter_pos` in the sells detector, explicitly seed `sells_X` EMA to 0 in the normative body, and state endpoint inclusivity for scheduler spans. `reaction_background` partial overlaps are representable as conservative whole-window demotion, but tests must pin that choice.

Beam-trade first plan is not implementation-ready because the P1 issue is design-level. The detector constants and overlap details are tuning/implementation-level; the temporal CG-BLIND rule is not.

NEEDS-ANOTHER-PASS: biggest blocker = CG-BLIND permits pre-impact structural prominence to read post-impact `damage/lethal/outcome`; design-level.

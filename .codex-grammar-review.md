SUCCESS: The process with PID 668876 (child process of PID 234088) has been terminated.
**P1 Findings**

- **Dramaturgy is demanding truth changes from a presentation stage.**  
  Primary: [Architecture](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:44>) says Layer 4 is “pure analysis of truth → arc labels”, but [Testing](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:151>) asserts initiative alternation, rising `mean_tier`, and `lethal.tick / duration >= LETHAL_LATE_FRAC`. A valid sim can produce a fast kill, a stomp, or non-rising weapon tiers. The choreographer cannot fix that without delaying/reordering outcome timing, which violates INV-CLOCKS/INV-ORDER. Make these diagnostics or reference-fixture expectations, not universal choreographer acceptance criteria.

- **Director-seam hooks have no deterministic identity model.**  
  Primary: [Boundary](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:85>) says per-beat hooks carry no `seq`, but the contract allows same-tick truth events whose meaning depends on `(tick, seq)`. Without a non-verified `truth_ref: {tick, seq}` or `beat_id`, `exchange_mode`, CRA windows, and `impact_frame` cannot be attached unambiguously. If these are instead added as fields on truth events, they risk entering canonical serialization unless the projection explicitly excludes them. Put hooks in a presentation namespace and update the loader/projection contract.

- **`FeelProfile` makes presentation determinism under-specified.**  
  Primary: [Architecture](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:39>) changes `stage()` to take optional `feel_profile?`, then [Data flow](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:108>) claims same `(truth, seed, feel_profile)` gives identical output. The frozen FeelProfile spec says it is not in the log and is re-derived from build stats. That is safe for combat truth, but not enough for reproducible PvP replay staging unless the build resolver, normalization constants, and preset table version are pinned and available. Optional fallback is also a branch hazard. Make profiles required per actor for staging, or explicitly accept that presentation logs are version-local.

- **CG-SPINE is over-promoted from useful regression metric to universal law.**  
  Primary: [The spine](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:26>) makes global speed bimodality invariant, and [Prosody](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:56>) says every layer enforces Pause-Burst-Pause. That will reject or distort legitimate sustained modes like Itano/swarm and dodge-pursuit, where the trace may be high-speed weave plus bearing oscillation rather than near-zero pauses. It is also gameable: injected holds can pass a histogram while spatial intent still reads wrong. Scope it to beam-trade/reference regression, and define mode-specific contrast metrics for swarm/pursuit.

- **“Every shot has non-zero CRA” will break on dense logs.**  
  Primary: [Testing](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:151>) requires every `shot` to get a CRA window. The contract permits close or same-tick events ordered by `seq`; high-rate weapons can generate overlapping beats. The new spec lacks the old choreographer’s explicit precedence/conflict model. First implementation needs a beat scheduler: coalesce shots, prioritize lethal/heavy beats, allow background shots without full CRA, and define what happens when one actor is already committed to a primitive.

**P2 Findings**

- **Extensibility claim conflicts with the shipped FeelProfile vocabulary.**  
  Primary: [Extensibility](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:114>) closes modes over `{beam-trade, swarm/Itano, dodge-pursuit, melee}` and says weapons provide a `mode_tag`. FeelProfile’s `mode_mix` is an open map with v1 `{ranged, melee, barrage}` plus reserved `evasion`. The mapping is not specified. A single closed `mode_tag` also fails hybrid weapons. Define a data mapping from feel modes to grammar-mode weights, allow per-weapon multi-mode weights, and move archetype presets out of `grammar_params.gd` if “never code” is meant literally.

- **The choreographer/director boundary is leaky.**  
  Primary: [Boundary](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:78>) says director owns cuts/framing/lens/grade, but the hook list includes `held_climax` with a suggested dwell multiplier and an `impact_frame` flag. Those are Timing/Cut/render behavior in the already-built director grammar. The choreographer should expose structural facts: phrase bounds, truth beat refs, CRA/fire/impact windows, range/mode labels, peripeteia tick. Let the director choose dwell, axis break, impact-frame rendering, and reaction-shot policy.

- **Traceability metrics are proxies, not proof.**  
  Primary: [Testing](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:143>) says tests assert principles, not pixels, but several signals are labels or weak proxies: `impact_frame` and smear need renderer/pose data, initiative can be tautological if generated and checked by the same algorithm, and range-band quantization can pass while composition still reads badly. The research caveat says thresholds are design constants, not literature. The spec needs a metric definition: sampling rate, histogram method, actor aggregation, tolerance, normalization by duration, and which failures are hard vs advisory.

**P3 Findings**

- **The decomposition is too much for “beam-trade first”.**  
  Primary: [Components](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:62>) lists seven files, not six, while [Scope](<D:/Claude/Mech Bags/docs/superpowers/specs/2026-06-20-choreography-grammar-design.md:129>) only tunes beam-trade. Registering three mode stubs, a primitive registry, separate arc/modes/prosody modules, and broad metrics before one path works is YAGNI. A leaner first pass: `choreographer.gd`, `beam_trade.gd`, `grammar_params`, and `movement_trace`; defer other mode files until they have real behavior.

No files were edited.
